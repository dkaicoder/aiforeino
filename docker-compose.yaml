services:
  aiforeino:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aiforeino
    ports:
      - "8080:8080"
    volumes:
      - "./data/aiforeino/config:/dist/config"
      - "./data/aiforeino/static:/dist/static"
    depends_on:
      - mysql
      - redis-stack
      - kafka
  redis-stack:
    image: redis/redis-stack:latest
    container_name: redis-stack
    ports:
      - "6973:6379"  # Redis port
      - "8001:8001"  # RedisInsight port
    volumes:
      - ./data/redis:/data
    environment:
      - REDIS_PASSWORD=redissuper2secret
      - REDIS_ARGS=--dir /data --appendonly no --save 1800 1 --bind 0.0.0.0
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 3
  kafka:
    image: apache/kafka:latest
    container_name: kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: 'broker,controller'
      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@localhost:9093'
      # 关键修改：添加 PLAINTEXT_HOST 监听器
      KAFKA_LISTENERS: 'PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093,PLAINTEXT_HOST://0.0.0.0:9094'
      # 分别给容器内部和宿主机返回不同的地址
      KAFKA_ADVERTISED_LISTENERS: 'PLAINTEXT://localhost:9092,PLAINTEXT_HOST://localhost:9092'
      KAFKA_CONTROLLER_LISTENER_NAMES: 'CONTROLLER'
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: 'CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT'
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
  mysql:
    image: mysql:latest
    container_name: mysql
    restart: always
    ports:
      - "33306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root123456
      MYSQL_DATABASE: aiforeino
      MYSQL_USER: root
      MYSQL_PASSWORD: 123456
    volumes:
      - "./data/mysql/data:/var/lib/mysql"
      - "./data/mysql/config:/etc/mysql/conf.d"

#  kafka-ui:
#    image: ghcr.io/kafbat/kafka-ui:latest
#    container_name: kafka-ui
#    ports:
#      - "8080:8080"
#    environment:
#      KAFKA_CLUSTERS_0_NAME: local-cluster
#      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:9092  # 这个是对的，不用改
#      DYNAMIC_CONFIG_ENABLED: 'true'
#      KAFKA_UI_DATA_BUFFER_LIMIT: 20MB
#    depends_on:
#      - kafka